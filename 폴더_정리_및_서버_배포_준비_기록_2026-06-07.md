# 폴더 정리 및 서버 배포 준비 기록 (2026-06-07)

## 배경

다른 사람(친구)이 서버에 올려 시연할 수 있도록, 프로젝트 폴더에서 개발 과정 중 생긴
임시·중간 산출물을 제거하고 배포에 필요한 파일을 정돈했다. 핵심 작업 결과(평가 결과,
실험 결론 등)는 이미 각 결과 파일에 반영되어 있으므로, 그 과정에서 생긴 중간 로그만 삭제 대상으로 판단했다.

---

## 1. 삭제한 임시 산출물 (13개)

`evaluate_pipeline.py` 개발·검증 과정에서 생성된 중간 실행 로그/백업으로,
최종 결과는 이미 `pipeline_v22/results/evaluate_pipeline_results.txt`에 반영되어 있어 불필요 판단.

| 파일 | 내용 | 삭제 사유 |
|---|---|---|
| `pipeline_v22/results/run_chunk1.log` ~ `run_chunk6.log` (6개) | evaluate_pipeline.py를 6개 청크로 나눠 실행한 중간 로그 (각 ~49KB) | 최종 accuracy가 이미 `evaluate_pipeline_results.txt`에 반영됨 |
| `pipeline_v22/results/rerun_cerebras.log` | Cerebras fallback 추가 후 재실행 로그 | 최종 결과에 반영 완료, 디버깅용 중간 로그 |
| `pipeline_v22/results/rerun_new_prompt.log`, `rerun_new_prompt_utf8.txt` | 프롬프트 변경 1차 실험 로그 (인코딩 깨진 원본 + UTF-8 변환본) | 프롬프트 튜닝 검증용 임시 로그, 최종 프롬프트는 `evaluate_pipeline.py`에 반영됨 |
| `pipeline_v22/results/rerun_new_prompt2.log`, `rerun_new_prompt2_utf8.txt` | 프롬프트 변경 2차 실험 로그 (원본 + UTF-8 변환본) | 위와 동일 사유 |
| `pipeline_v22/llm_cache_old_prompt_backup.json` | 이전 프롬프트 버전의 LLM 캐시 응답 백업 | 현재 `llm_cache.json`으로 교체 완료, 더 이상 불필요 |
| `실험/ood_train_samples_decoded.txt` | OOD 학습 샘플을 라벨별로 디코딩해 출력한 텍스트 | 임시 디버깅·확인용 산출물 |

---

## 2. 배포 준비를 위해 수정한 파일

### `pipeline_v22/evaluate_pipeline.py`
- LLM fallback 순위에 **Cerebras(gpt-oss-120b)**, **OpenRouter(gpt-oss-120b:free)** 추가
  → 기존 Gemini → Groq 2단계에서, API 할당량 소진 시 더 안정적으로 평가를 이어갈 수 있도록 보강
- 분류 프롬프트(`_SYS`)에 다음 두 가지 가이드를 추가:
  - "단계어"(관심/주의/경보/심각 등)에 따라 같은 재난 소재라도 L0~L4 전 구간에 분포할 수 있다는 점
  - 위협 표현이 없는 행정·정보성 공지(정전 안내, 동선 안내, 해제 공지 등)는 기본 L0으로 분류

### `중요파일/hf_space/app.py`, `Dockerfile`, `requirements.txt`
시연 서버(친구가 올릴 서버)에서 **v22 모델 + KNN-OOD 탐지 + LLM fallback이 통합된 최종 파이프라인**이
그대로 동작하도록 데모 API를 갱신했다.

- `app.py`: 기존 "HF Hub에서 모델 다운로드" 방식에서 **로컬 `model/`·`tokenizer/` 폴더 + KNN-OOD 인덱스
  (`knn_ood_v22.npz`, `knn_ood_v22_meta.pt`)를 함께 패키징해 구동**하는 방식으로 변경.
  LLM fallback도 OpenRouter → Gemini → Groq → Cerebras 순으로 확장(모두 실패 시 v22 단독 예측 사용)
- `Dockerfile`: `requirements.txt` 기반 설치로 변경, 모델·토크나이저·OOD 인덱스 파일을 이미지에 COPY하도록 수정
- `requirements.txt`: `scikit-learn`, `requests`, `google-genai`, `groq` 추가 (KNN-OOD 및 LLM fallback 의존성)

> 참고: `model/`, `tokenizer/`, `knn_ood_v22.npz`, `knn_ood_v22_meta.pt`는 용량이 커서
> `.gitignore`에 의해 git 추적 대상이 아니다. 친구에게 폴더를 전달할 때 `중요파일/hf_space/` 안에
> 이 파일들이 실제로 존재하는 상태(현재 로컬에 준비 완료)로 함께 넘기면 된다.
> LLM API 키는 서버의 환경변수(`GEMINI_API_KEY` / `GROQ_API_KEY` / `CEREBRAS_API_KEY` / `OPENROUTER_API_KEY`)로 등록해야 한다.

---

## 정리 결과

위 13개 임시 파일 삭제 후 작업 트리에는 의도한 수정 사항만 남았다 (불필요한 untracked 파일 0개).
이제 `중요파일/hf_space/` 폴더를 그대로 전달하면 친구가 서버에 올려 시연할 수 있는 상태다.
